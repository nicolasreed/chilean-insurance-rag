# Chilean Insurance RAG

Under active development, building in public.

A RAG system for querying Chilean insurance regulation (CMF) with mandatory citations.

## Setup
1. `uv sync`
2. Copy `.env.example` to `.env` and fill in your tokens
3. `uv run scripts/smoke_test.py` to verify connection